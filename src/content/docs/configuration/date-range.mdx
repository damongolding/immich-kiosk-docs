---
title: Date Range
---

import { Aside } from '@astrojs/starlight/components';
import Settings from "../../../components/config-settings.astro";


    Date ranges are used to define distinct collections/pools of assets based on their timestamps. Unlike filters that modify existing collections/pools, each date range defines its own independent set of assets.

    If you are looking to modify existing collections/pools, consider using [filters instead](/configuration/filters/#date-filter).

## Date Range

A date range or ranges. See Date range for more information.

<Aside type="tip">
    You can use `today` as an alias for the current date.

    Examples:

    `http://{URL}?date=today`

    `http://{URL}?date=2023-01-01_to_today`
</Aside>

<Settings
    config="dates"
    env="KIOSK_DATES"
    url="date"
    value="[]string"
    defaultValue="[]"
    configOver={`dates:
    - 2023-01-01_to_2023-02-01
    - 2024-11-12_to_2023-11-18
    - last-30-days
    - today
    `}
    envOver={`environment:
    KIOSK_DATES: "DATE_RANGE, DATE_RANGE, DATE_RANGE"
    `}
    urlOver={"http://{URL}?date=DATE_RANGE&date=DATE_RANGE&date=DATE_RANGE"}
/>

### How date ranges work as asset buckets
Date ranges in Immich Kiosk create distinct pools (or "buckets") of assets based on their timestamps.
Unlike filters that modify existing collections, each date range defines its own independent set of assets.
When you specify multiple date ranges, Kiosk maintains separate buckets for each range and randomly selects
one bucket during image refresh to fetch an asset from.

### Allowed formats
- `YYYY-MM-DD_to_YYYY-MM-DD` e.g.2023-01-01_to_2023-02-01
- `last-XX-days` e.g. last-30-days
