---
title: Configuration
sidebar:
  order: 1
---

import { Aside } from '@astrojs/starlight/components';
import { Code } from "@astrojs/starlight/components";

export const config = await fetch("https://raw.githubusercontent.com/damongolding/immich-kiosk/refs/heads/main/config.example.yaml");
export const configExample = await config.text();

export const compose = await fetch("https://raw.githubusercontent.com/damongolding/immich-kiosk/refs/heads/main/docker-compose.yaml");
export const composeExample = await compose.text();

export const composeEnv = await fetch("https://raw.githubusercontent.com/damongolding/immich-kiosk/refs/heads/main/docker-compose-env.yaml");
export const composeEnvExample = await composeEnv.text();

## docker-compose.yaml

<Aside type="note">
You can use both a `config.yaml` file and environment variables but environment variables will overwrite settings from the `config.yaml` file
Set `Lang` to a `language code` from [this list](/assets/locales.md))
Set `TZ` to a `TZ identifier` from [this list](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List)
</Aside>

<Code code={composeExample} lang="yaml" title="docker-compose.yaml" />


## config.yaml

<Code code={configExample} lang="yaml" title="config.yaml" />

## docker-compose.yaml (with env)

<Aside type="tip">
You do not need to specifiy all of these.
If you want the default behaviour/value you can omit it from you compose file.
</Aside>

<Code code={composeEnvExample} lang="yaml" title="docker-compose.yaml" />


## When checking `config.yaml` into a repository

It is recommended to avoid checking-in any secrets.

  1. Create a `docker-compose.env` file.

     ```sh
     touch docker-compose.env
     ```

  2. Remove secrets from `config.yaml` (and `docker-compose.yaml`) - generally `immich_url` and `immich_api_key`.

  3. Include your secrets in `docker-compose.env`.

     ```sh
     KIOSK_IMMICH_API_KEY=SECRET_KEY
     KIOSK_IMMICH_URL=SECRET_URL
     ```

  4. Update `docker-compose.yaml` to include `env_file:`.

     ```yaml
     services:
       immich-kiosk:
         env_file:
           - docker-compose.env
     ```

  5. Add `docker-compose.env` to `.gitignore`.


## Core

---

### immich_api_key

|         |                        |
| ------- | ---------------------- |
| CONFIG  | `immich_api_key`       |
| ENV     | `KIOSK_IMMICH_API_KEY` |
| VALUE   | `string`               |
| DEFAULT | `""`                   |

     The API for your Immich server.

---

### immich_url

|         |                    |
| ------- | ------------------ |
| CONFIG  | `immich_url`       |
| ENV     | `KIOSK_IMMICH_URL` |
| VALUE   | `string`           |
| DEFAULT | `""`               |

     The URL of your Immich server. MUST include a port if one is needed e.g. `http://192.168.1.123:2283`.

---

### immich_external_url

|         |                             |
| ------- | --------------------------- |
| CONFIG  | `immich_external_url`       |
| ENV     | `KIOSK_IMMICH_EXTERNAL_URL` |
| VALUE   | `string`                    |
| DEFAULT | `""`                        |

     The public URL of your Immich server used for generating links and QR codes in the additional information overlay. Useful when accessing Immich through a reverse proxy or different external URL. Example: \"https://photos.example.com\". If not set, falls back to immich_url.

---

## Clock

---

### show_time

|         |                   |
| ------- | ----------------- |
| CONFIG  | `show_time`       |
| ENV     | `KIOSK_SHOW_TIME` |
| VALUE   | `bool`            |
| DEFAULT | `false`           |

     Display clock.

---

### time_format

|         |                     |
| ------- | ------------------- |
| CONFIG  | `time_format`       |
| ENV     | `KIOSK_TIME_FORMAT` |
| VALUE   | `24 \| 12`          |
| DEFAULT | `24`                |

     Display clock time in either 12-hour or 24-hour format. This can either be 12 or 24.

---

### show_date

|         |                   |
| ------- | ----------------- |
| CONFIG  | `show_date`       |
| ENV     | `KIOSK_SHOW_DATE` |
| VALUE   | `bool`            |
| DEFAULT | `false`           |

     Display the date.

---

### date_format

|         |                     |
| ------- | ------------------- |
| CONFIG  | `date_format`       |
| ENV     | `KIOSK_DATE_FORMAT` |
| VALUE   | `string`            |
| DEFAULT | `DD/MM/YYYY`        |

     The format of the date. default is day/month/year. See date format for more information.

---

### clock_source

|         |                      |
| ------- | -------------------- | ------- |
| CONFIG  | `clock_source`       |
| ENV     | `KIOSK_CLOCK_SOURCE` |
| VALUE   | `client /            | server` |
| DEFAULT | "client"             |

     The source of the clock. Either client or server.

---

## Behavior

---

### refresh

|         |                 |
| ------- | --------------- |
| CONFIG  | `refresh`       |
| ENV     | `KIOSK_REFRESH` |
| VALUE   | `int`           |
| DEFAULT | `60`            |

     The amount in seconds a image will be displayed for.

---

### disable_screensaver

|         |                             |
| ------- | --------------------------- |
| CONFIG  | `disable_screensaver`       |
| ENV     | `KIOSK_DISABLE_SCREENSAVER` |
| VALUE   | `bool`                      |
| DEFAULT | `false`                     |

     Ask browser to request a lock that prevents device screens from dimming or locking. NOTE: I haven't been able to get this to work constantly on IOS.

---

### optimize_images

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `optimize_images`       |
| ENV     | `KIOSK_OPTIMIZE_IMAGES` |
| VALUE   | `bool`                  |
| DEFAULT | `false`                 |

     Whether Kiosk should resize images to match your browser screen dimensions for better performance. NOTE: In most cases this is not necessary, but if you are accessing Kiosk on a low-powered device, this may help.

---

### use_gpu

|         |                 |
| ------- | --------------- |
| CONFIG  | `use_gpu`       |
| ENV     | `KIOSK_USE_GPU` |
| VALUE   | `bool`          |
| DEFAULT | `true`          |

     Enable GPU acceleration for improved performance (e.g., CSS transforms)"

---

### show_archived

|         |                       |
| ------- | --------------------- |
| CONFIG  | `show_archived`       |
| ENV     | `KIOSK_SHOW_ARCHIVED` |
| VALUE   | `bool`                |
| DEFAULT | `false`               |

     Allow assets marked as archived to be displayed.

---

## Albums

---

### album

|         |               |
| ------- | ------------- |
| CONFIG  | `album`       |
| ENV     | `KIOSK_ALBUM` |
| VALUE   | `[]string`    |
| DEFAULT | `[]`          |

     The ID(s) of a specific album or albums you want to display. See Albums for more information.

---

### album_order

|         |                     |
| ------- | ------------------- | ------ | ------- |
| CONFIG  | `album_order`       |
| ENV     | `KIOSK_ALBUM_ORDER` |
| VALUE   | `random             | newest | oldest" |
| DEFAULT | "random"            |

     The order an album's assets will be displayed. See Album order for more information.

---

### excluded_albums

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `excluded_albums`       |
| ENV     | `KIOSK_EXCLUDED_ALBUMS` |
| VALUE   | `[]string`              |
| DEFAULT | `[]`                    |

     The ID(s) of a specific album or albums you want to exclude. See Exclude albums for more information.

---

### experimental_album_video

|         |                                  |
| ------- | -------------------------------- |
| CONFIG  | `experimental_album_video`       |
| ENV     | `KIOSK_EXPERIMENTAL_ALBUM_VIDEO` |
| VALUE   | `bool`                           |
| DEFAULT | `false`                          |

     Enable experimental video playback for albums. See experimental album video for more information.

---

## People

---

### person

|         |                |
| ------- | -------------- |
| CONFIG  | `person`       |
| ENV     | `KIOSK_PERSON` |
| VALUE   | `[]string`     |
| DEFAULT | `[]`           |

     The ID(s) of a specific person or people you want to display. See People for more information.

---

### require_all_people

|         |                            |
| ------- | -------------------------- |
| CONFIG  | `require_all_people`       |
| ENV     | `KIOSK_REQUIRE_ALL_PEOPLE` |
| VALUE   | `bool`                     |
| DEFAULT | `false`                    |

     Require all people to be present in an asset. See Require all people for more information.

---

### excluded_people

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `excluded_people`       |
| ENV     | `KIOSK_EXCLUDED_PEOPLE` |
| VALUE   | `[]string`              |
| DEFAULT | `[]`                    |

     The ID(s) of a specific person or people you want to exclude. See Exclude people for more information.

---

## Date range

---

### date

|         |              |
| ------- | ------------ |
| CONFIG  | `date`       |
| ENV     | `KIOSK_DATE` |
| VALUE   | `[]string`   |
| DEFAULT | `[]`         |

     A date range or ranges. See Date range for more information.

---

## Tags

---

### tag

|         |             |
| ------- | ----------- |
| CONFIG  | `tag`       |
| ENV     | `KIOSK_TAG` |
| VALUE   | `[]string`  |
| DEFAULT | `[]`        |

     Tag or tags you want to display. See Tags for more information.

---

## Memories

---

### memories

|         |                  |
| ------- | ---------------- |
| CONFIG  | `memories`       |
| ENV     | `KIOSK_MEMORIES` |
| VALUE   | `bool`           |
| DEFAULT | `false`          |

     Display memories.

---

### blacklist

|         |                   |
| ------- | ----------------- |
| CONFIG  | `blacklist`       |
| ENV     | `KIOSK_BLACKLIST` |
| VALUE   | `[]string`        |
| DEFAULT | `[]`              |

     The ID(s) of any specific assets you want Kiosk to skip/exclude from displaying. You can also tag assets in Immich with \"kiosk-skip\" to achieve the same.

---

## Filters

---


### date_filter

|         |                     |
| ------- | ------------------- |
| CONFIG  | `date_filter`       |
| ENV     | `KIOSK_DATE_FILTER` |
| VALUE   | `string`            |
| DEFAULT | `""`                |

     Filter person and random assets by date. See date filter for more information.

---

## User interface

---

### disable_navigation

|         |                            |
| ------- | -------------------------- |
| CONFIG  | `disable_navigation`       |
| ENV     | `KIOSK_DISABLE_NAVIGATION` |
| VALUE   | `bool`                     |
| DEFAULT | `false`                    |

     Disable all Kiosk's navigation (touch/click, keyboard and menu).

---

### disable_ui

|         |                    |
| ------- | ------------------ |
| CONFIG  | `disable_ui`       |
| ENV     | `KIOSK_DISABLE_UI` |
| VALUE   | `bool`             |
| DEFAULT | `false`            |

     A shortcut to set show_time, show_date, show_image_time and image_date_format to false.

---

### frameless

|         |                   |
| ------- | ----------------- |
| CONFIG  | `frameless`       |
| ENV     | `KIOSK_FRAMELESS` |
| VALUE   | `bool`            |
| DEFAULT | `false`           |

     Remove borders and rounded corners on images.

---

### hide_cursor

|         |                     |
| ------- | ------------------- |
| CONFIG  | `hide_cursor`       |
| ENV     | `KIOSK_HIDE_CURSOR` |
| VALUE   | `bool`              |
| DEFAULT | `false`             |

     Hide cursor/mouse via CSS.

---

### font_size

|         |                   |
| ------- | ----------------- |
| CONFIG  | `font_size`       |
| ENV     | `KIOSK_FONT_SIZE` |
| VALUE   | `int`             |
| DEFAULT | 100               |

     The base font size for Kiosk. Default is 100% (16px). DO NOT include the % character.

---

### background_blur

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `background_blur`       |
| ENV     | `KIOSK_BACKGROUND_BLUR` |
| VALUE   | `bool`                  |
| DEFAULT | `true`                  |

     Display a blurred version of the image as a background.

---

### background_blur_amount

|         |                                |
| ------- | ------------------------------ |
| CONFIG  | `background_blur_amount`       |
| ENV     | `KIOSK_BACKGROUND_BLUR_AMOUNT` |
| VALUE   | `int`                          |
| DEFAULT | 10                             |

     The amount of blur to apply to the background image (sigma).

---

### theme

|         |                  |
| ------- | ---------------- |
| CONFIG  | `theme`          |
| ENV     | `KIOSK_THEME`    |
| VALUE   | `fade  \| solid` |
| DEFAULT | "fade"           |

     Which theme to use. See Themes for more information.

---

### layout

|         |                                                                       |
| ------- | --------------------------------------------------------------------- |
| CONFIG  | `layout`                                                              |
| ENV     | `KIOSK_LAYOUT`                                                        |
| VALUE   | `single \| portrait \| landscape \| splitview \| splitview-landscape` |
| DEFAULT | `""`                                                                  |

     Which layout to use. See Layouts for more information.

---

## Sleep mode

---

### sleep_start

|         |                     |
| ------- | ------------------- |
| CONFIG  | `sleep_start`       |
| ENV     | `KIOSK_SLEEP_START` |
| VALUE   | `string`            |
| DEFAULT | `""`                |

     Time (in 24hr format) to start sleep mode. See Sleep mode for more information.

---

### sleep_end

|         |                   |
| ------- | ----------------- |
| CONFIG  | `sleep_end`       |
| ENV     | `KIOSK_SLEEP_END` |
| VALUE   | `string`          |
| DEFAULT | `""`              |

     Time (in 24hr format) to end sleep mode. See Sleep mode for more information.

---

### sleep_icon

|         |                    |
| ------- | ------------------ |
| CONFIG  | `sleep_icon`       |
| ENV     | `KIOSK_SLEEP_ICON` |
| VALUE   | `string`           |
| DEFAULT | `""`               |

     Display icon during sleep mode. See Sleep mode for more information.

---

### sleep_dim_screen

|         |                          |
| ------- | ------------------------ |
| CONFIG  | `sleep_dim_screen`       |
| ENV     | `KIOSK_SLEEP_DIM_SCREEN` |
| VALUE   | `bool`                   |
| DEFAULT | `false`                  |

     Dim screen during sleep mode when using Fully Kiosk Browser. See Sleep mode for more information.

---

### disable_sleep

|         |                 |
| ------- | --------------- |
| CONFIG  | `disable_sleep` |
| ENV     | `N`A"           |
| VALUE   | `bool`          |
| DEFAULT | `false`         |

     Bypass sleep mode by adding `disable_sleep=true` to the URL. See Sleep mode for more information.

---

## Custom CSS

---

### custom_css

|         |              |
| ------- | ------------ |
| CONFIG  | `custom_css` |
| ENV     | `N`A"        |
| VALUE   | `bool`       |
| DEFAULT | `true`       |

     Allow custom CSS to be used. See Custom CSS for more information.

---

### transition

|         |                    |
| ------- | ------------------ | ---- | ----------- |
| CONFIG  | `transition`       |
| ENV     | `KIOSK_TRANSITION` |
| VALUE   | `none              | fade | cross-fade" |
| DEFAULT | "none"             |

     Which transition to use when changing images.

---

### fade_transition_duration

|         |                                  |
| ------- | -------------------------------- |
| CONFIG  | `fade_transition_duration`       |
| ENV     | `KIOSK_FADE_TRANSITION_DURATION` |
| VALUE   | `float"                          |
| DEFAULT | 1                                |

     The duration of the fade (in seconds) transition.

---

### cross_fade_transition_duration

|         |                                        |
| ------- | -------------------------------------- |
| CONFIG  | `cross_fade_transition_duration`       |
| ENV     | `KIOSK_CROSS_FADE_TRANSITION_DURATION` |
| VALUE   | `float"                                |
| DEFAULT | 1                                      |

     The duration of the cross-fade (in seconds) transition.

---

### show_progress

|         |                       |
| ------- | --------------------- |
| CONFIG  | `show_progress`       |
| ENV     | `KIOSK_SHOW_PROGRESS` |
| VALUE   | `bool`                |
| DEFAULT | `false`               |

     Display a progress bar for when image will refresh.

---

### image_fit

|         |                   |
| ------- | ----------------- | ----- | ----- |
| CONFIG  | `image_fit`       |
| ENV     | `KIOSK_IMAGE_FIT` |
| VALUE   | `contain          | cover | none" |
| DEFAULT | "contain"         |

     How your image will fit on the screen. Default is contain. See Image fit for more info.

---

### image_effect

|         |                      |
| ------- | -------------------- | ---- | ----------- |
| CONFIG  | `image_effect`       |
| ENV     | `KIOSK_IMAGE_EFFECT` |
| VALUE   | `none                | zoom | smart-zoom" |
| DEFAULT | "none"               |

     Add an effect to images.

---

### image_effect_amount

|         |                             |
| ------- | --------------------------- |
| CONFIG  | `image_effect_amount`       |
| ENV     | `KIOSK_IMAGE_EFFECT_AMOUNT` |
| VALUE   | `int`                       |
| DEFAULT | 120                         |

     Set the intensity of the image effect. Use a number between 100 (minimum) and higher, without the % symbol.

---

### use_original_image

|         |                            |
| ------- | -------------------------- |
| CONFIG  | `use_original_image`       |
| ENV     | `KIOSK_USE_ORIGINAL_IMAGE` |
| VALUE   | `bool`                     |
| DEFAULT | `false`                    |

     Use the original image. NOTE: If the original is not a png, gif, jpeg or webp Kiosk will fallback to using the preview.

---

### show_owner

|         |                    |
| ------- | ------------------ |
| CONFIG  | `show_owner`       |
| ENV     | `KIOSK_SHOW_OWNER` |
| VALUE   | `bool`             |
| DEFAULT | `false`            |

     Display the asset owner. Useful for shared albums.

---

### show_album_name

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `show_album_name`       |
| ENV     | `KIOSK_SHOW_ALBUM_NAME` |
| VALUE   | `bool`                  |
| DEFAULT | `false`                 |

     Display album name(s) that the asset appears in.

---

### show_person_name

|         |                          |
| ------- | ------------------------ |
| CONFIG  | `show_person_name`       |
| ENV     | `KIOSK_SHOW_PERSON_NAME` |
| VALUE   | `bool`                   |
| DEFAULT | `false`                  |

     Display person name(s).

---

### show_person_age

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `show_person_age`       |
| ENV     | `KIOSK_SHOW_PERSON_AGE` |
| VALUE   | `bool`                  |
| DEFAULT | `false`                 |

     Display person age.

---

### show_image_time

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `show_image_time`       |
| ENV     | `KIOSK_SHOW_IMAGE_TIME` |
| VALUE   | `bool`                  |
| DEFAULT | `false`                 |

     Display image time from METADATA (if available).

---

### image_time_format

|         |                           |
| ------- | ------------------------- |
| CONFIG  | `image_time_format`       |
| ENV     | `KIOSK_IMAGE_TIME_FORMAT` |
| VALUE   | `12 \| 24`                |
| DEFAULT | `24`                      |

     Display image time in either 12-hour or 24-hour format. This can either be 12 or 24.

---

### show_image_date

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `show_image_date`       |
| ENV     | `KIOSK_SHOW_IMAGE_DATE` |
| VALUE   | `bool`                  |
| DEFAULT | `false`                 |

     Display the image date from METADATA (if available).

---

### image_date_format

|         |                           |
| ------- | ------------------------- |
| CONFIG  | `image_date_format`       |
| ENV     | `KIOSK_IMAGE_DATE_FORMAT` |
| VALUE   | `string`                  |
| DEFAULT | `DD/MM/YYYY`              |

     The format of the image date. default is day/month/year. See date format for more information.

---

### show_image_description

|         |                                |
| ------- | ------------------------------ |
| CONFIG  | `show_image_description`       |
| ENV     | `KIOSK_SHOW_IMAGE_DESCRIPTION` |
| VALUE   | `bool`                         |
| DEFAULT | `false`                        |

     Display image description from METADATA (if available).

---

### show_image_exif

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `show_image_exif`       |
| ENV     | `KIOSK_SHOW_IMAGE_EXIF` |
| VALUE   | `bool`                  |
| DEFAULT | `false`                 |

     Display image Fnumber, Shutter speed, focal length, ISO from METADATA (if available).

---

### show_image_location

|         |                             |
| ------- | --------------------------- |
| CONFIG  | `show_image_location`       |
| ENV     | `KIOSK_SHOW_IMAGE_LOCATION` |
| VALUE   | `bool`                      |
| DEFAULT | `false`                     |

     Display the image location from METADATA (if available).

---

### show_image_qr

|         |                       |
| ------- | --------------------- |
| CONFIG  | `show_image_qr`       |
| ENV     | `KIOSK_SHOW_IMAGE_QR` |
| VALUE   | `bool`                |
| DEFAULT | `false`               |

     Displays a QR code linking to the original image (in Immich) next to the image metadata.

---

### hide_countries

|         |                        |
| ------- | ---------------------- |
| CONFIG  | `hide_countries`       |
| ENV     | `KIOSK_HIDE_COUNTRIES` |
| VALUE   | `[]string`             |
| DEFAULT | `[]`                   |

     List of countries to hide from image_location"

---

### show_more_info

|         |                        |
| ------- | ---------------------- |
| CONFIG  | `show_more_info`       |
| ENV     | `KIOSK_SHOW_MORE_INFO` |
| VALUE   | `bool`                 |
| DEFAULT | `true`                 |

     Enables the display of additional information about the current image(s)"

---

### show_more_info_image_link

|         |                                   |
| ------- | --------------------------------- |
| CONFIG  | `show_more_info_image_link`       |
| ENV     | `KIOSK_SHOW_MORE_INFO_IMAGE_LINK` |
| VALUE   | `bool`                            |
| DEFAULT | `true`                            |

     Shows a link to the original image (in Immich) in the additional information overlay"

---

### show_more_info_qr_code

|         |                                |
| ------- | ------------------------------ |
| CONFIG  | `show_more_info_qr_code`       |
| ENV     | `KIOSK_SHOW_MORE_INFO_QR_CODE` |
| VALUE   | `bool`                         |
| DEFAULT | `true`                         |

     Displays a QR code linking to the original image (in Immich) in the additional information overlay"

---

### like_button_action

|         |                            |
| ------- | -------------------------- |
| CONFIG  | `like_button_action`       |
| ENV     | `KIOSK_LIKE_BUTTON_ACTION` |
| VALUE   | `[]string`                 |
| DEFAULT | `["favorite"]`             |

     Action(s) to perform when the like button is clicked. Supported actions are [favorite, album]. See like button for more information.

---

### hide_button_action

|         |                            |
| ------- | -------------------------- |
| CONFIG  | `hide_button_action`       |
| ENV     | `KIOSK_HIDE_BUTTON_ACTION` |
| VALUE   | `[]string`                 |
| DEFAULT | `["tag"]`                  |

     Action(s) to perform when the hide button is clicked. Supported actions are [tag, archive]. See hide button for more information.

---

### immich_users_api_keys

|         |                         |
| ------- | ----------------------- |
| CONFIG  | `immich_users_api_keys` |
| ENV     | `N`A"                   |
| VALUE   | `map[string]string"     |
| DEFAULT | `{}`                    |

     key:value mappings of Immich usernames to their corresponding API keys. See multiple users for more information.

---

### show_user

|         |                   |
| ------- | ----------------- |
| CONFIG  | `show_user`       |
| ENV     | `KIOSK_SHOW_USER` |
| VALUE   | `bool`            |
| DEFAULT | `false`           |

     Display the user used to fetch the image. See multiple users for more information.

---
## Weather

<Aside type="caution">
    This feature can only be configured using a `config.yaml` file.
</Aside>

<Aside type="note">
    To use the weather feature, you’ll need an API key from [OpenWeatherMap](https://openweathermap.org).
</Aside>

<Aside type="tip">
    OpenWeatherMap limits API usage to 60 calls per hour.
    Since the kiosk refreshes weather data every 10 minutes, you can monitor up to 6 locations with a single API key.
</Aside>


|         |                     |
| ------- | ------------------- |
| CONFIG  | `weather`           |
| ENV     | `N/A`               |
| VALUE   | `[]WeatherLocation` |
| DEFAULT | `[]`                |

### Setting Up Weather Locations

You can configure multiple locations in the `config.yaml` file, and choose which one to display using the URL query `weather=NAME`.

### Weather Location Configuration Options:

<Aside type="tip">
If you would prefer to use the location name from OpenWeatherMap's API, add "-api" to the end of the name.
e.g. `name: london-api`
</Aside>

| **Value**   | **Description** |
|-------------|-----------------|
| name        | The location’s display name (used in the URL query). |
| lat         | Latitude of the location. |
| lon         | Longitude of the location. |
| api         | OpenWeatherMap API key. |
| unit        | Units of measurement (`standard`, `metric`, or `imperial`). |
| lang        | Language code for weather descriptions (see the full list [here](https://openweathermap.org/current#multi)). |
| default     | Set this location as the default (when no location is specified) |

### Example Configuration

Here’s an example of how to add London, London using OpenWeatherMap's name and New York to the config.yaml file. These locations would be selectable via the URL, like this:
`http://{URL}?weather=london or http://{URL}?weather=new-york.`

```yaml
 weather:
  - name: london
    lat: 51.5285262
    lon: -0.2663999
    api: API_KEY
    unit: metric
    lang: en
    default: true

  - name: london-api
    lat: 51.5285262
    lon: -0.2663999
    api: API_KEY
    unit: metric
    lang: en
    default: true

  - name: new-york
    lat: 40.6973709
    lon: -74.1444838
    api: API_KEY
    unit: imperial
    lang: en
```

---

## Offline mode

---

### use_offline_mode

|         |                          |
| ------- | ------------------------ |
| CONFIG  | `use_offline_mode`       |
| ENV     | `KIOSK_USE_OFFLINE_MODE` |
| VALUE   | `bool`                   |
| DEFAULT | `false`                  |

Enable offline mode for the device. See offline mode for more information.

---

### offline_mode

|         |                 |
| ------- | --------------- |
| CONFIG  | `offline_mode`  |
| ENV     | `N`A"           |
| VALUE   | `OfflineMode{}" |
| DEFAULT | `{}`            |

Enable offline mode. See offline mode for more information.

---

## iframe

---

### iframe

|         |                |
| ------- | -------------- |
| CONFIG  | `iframe`       |
| ENV     | `KIOSK_IFRAME` |
| VALUE   | `[]string`     |
| DEFAULT | `[]`           |

     Add iframes into Kiosk. See iframe for more information.

---
